# Production environment configuration for Fisher App
# Since all sub-apps are hosted under the same Vercel domain on sub-paths:
# Admin: /admin/
# Fisher: /fisher/
# Marketplace: /market/
# We can use a relative root path for the marketplace URL to avoid hardcoding!

VITE_MARKETPLACE_URL=/market/
